import { VerifyEmailScreen } from '@ridemesh/mobile-auth';
import { theme } from '../src/theme';

export default function VerifyEmail() {
  return <VerifyEmailScreen app="passenger" theme={theme} />;
}
