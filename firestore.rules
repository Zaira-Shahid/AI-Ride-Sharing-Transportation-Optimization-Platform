rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client access until role-based rules are implemented with authentication.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
