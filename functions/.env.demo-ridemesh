# Read by the emulators the automated tests start (project demo-ridemesh) and by nothing else.
# Reverse geocoding goes to a fake Nominatim, and routes to a fake OSRM, that the tests run themselves
# (tests/fake-nominatim.ts, tests/fake-osrm.ts).
NOMINATIM_BASE_URL=http://127.0.0.1:18889
GEOCODING_USER_AGENT=RideMesh-tests
GEOCODING_MIN_SPACING_MS=0
ROUTING_BASE_URL_DRIVING=http://127.0.0.1:18890/routed-car
ROUTING_BASE_URL_WALKING=http://127.0.0.1:18890/routed-foot
ROUTING_MIN_SPACING_MS=0
