# Read by the emulators the automated tests start (project demo-ridemesh) and by nothing else.
# Reverse geocoding goes to a fake Nominatim the tests run themselves (tests/fake-nominatim.ts).
NOMINATIM_BASE_URL=http://127.0.0.1:18889
GEOCODING_USER_AGENT=RideMesh-tests
GEOCODING_MIN_SPACING_MS=0
